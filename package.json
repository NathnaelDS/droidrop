{
  "name": "droidrop",
  "version": "0.1.0",
  "description": "Mac <-> Android file transfer over ADB (USB or wifi), no app needed on the phone",
  "type": "module",
  "scripts": {
    "start": "node src/server.js"
  },
  "engines": {
    "node": ">=20"
  }
}
